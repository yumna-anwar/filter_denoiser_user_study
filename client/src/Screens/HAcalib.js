import React, { useState, useEffect, useRef } from "react";
import { useNavigate } from "react-router-dom";
import Header from "../Components/Header";

const HAcalib = () => {
  const navigate = useNavigate();
  return (
    <>
      <Header />
      #SCREEN HTML GOES HERE


    </>
    );
  };

  export default HAcalib;
